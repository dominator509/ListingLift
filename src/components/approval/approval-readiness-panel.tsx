type Props = { jobId: string; blockers: string[]; warnings: string[]; requiredActions: string[]; canApprove: boolean };
export function ApprovalReadinessPanel({ jobId, blockers, warnings, requiredActions, canApprove }: Props) {
  return <section className="rounded-2xl border bg-white p-6 shadow-sm"><div className="flex items-start justify-between gap-4"><div><p className="text-sm font-medium text-slate-500">Job {jobId}</p><h2 className="text-xl font-semibold text-slate-950">Approval readiness</h2><p className="mt-2 text-sm text-slate-600">Manual approval is required before any final delivery can become client-visible.</p></div><span className={`rounded-full px-3 py-1 text-xs font-medium ${canApprove ? 'bg-emerald-100 text-emerald-700' : 'bg-amber-100 text-amber-700'}`}>{canApprove ? 'Ready' : 'Blocked'}</span></div><div className="mt-5 grid gap-4 md:grid-cols-3"><List title="Blockers" items={blockers} empty="No hard blockers in this draft." /><List title="Warnings" items={warnings} empty="No warnings in this draft." /><List title="Required actions" items={requiredActions} empty="No additional actions listed." /></div></section>;
}
function List({ title, items, empty }: { title: string; items: string[]; empty: string }) { return <div className="rounded-xl border bg-slate-50 p-4"><h3 className="text-sm font-semibold text-slate-900">{title}</h3>{items.length ? <ul className="mt-3 list-disc space-y-1 pl-4 text-sm text-slate-600">{items.map((item) => <li key={item}>{item}</li>)}</ul> : <p className="mt-3 text-sm text-slate-500">{empty}</p>}</div>; }
