# Phase 30 Gap Handoff

Codex must complete runtime work that ChatGPT Project Mode cannot verify:

- Install dependencies and validate types.
- Generate and apply real migrations.
- Wire tenant-scoped Prisma persistence.
- Implement encrypted secret references.
- Implement real provider calls behind feature flags.
- Add queue/retry/manual fallback runtime behavior.
- Verify UI routes in browser.
- Run all required tests/checks.
